name: 🚀 Deploy to Production v23

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: 🔐 SSH & Deploy to Server
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3