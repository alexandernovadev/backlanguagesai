version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: languageai-backend
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      # Persistir logs
      - ./logs:/app/logs
      # Persistir imágenes/audios subidos
      - ./public:/app/public
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Opcional: MongoDB local (si no usas Atlas)
  # mongodb:
  #   image: mongo:7
  #   container_name: languageai-mongo
  #   restart: unless-stopped
  #   ports:
  #     - "27017:27017"
  #   environment:
  #     - MONGO_INITDB_ROOT_USERNAME=admin
  #     - MONGO_INITDB_ROOT_PASSWORD=changeme
  #   volumes:
  #     - mongodb_data:/data/db
  #   networks:
  #     - app-network

networks:
  app-network:
    driver: bridge

# volumes:
#   mongodb_data:
